% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{compute_cluster}
\alias{compute_cluster}
\title{Compute hierarchical clustering}
\usage{
compute_cluster(
  mat,
  direction = c("row", "col"),
  dist_method = "euclidean",
  hclust_method = "complete",
  dist_fn = NULL
)
}
\arguments{
\item{mat}{Numeric matrix.}

\item{direction}{\code{"row"} or \code{"col"}.}

\item{dist_method}{Distance method passed to \code{\link[stats:dist]{stats::dist()}}.
Additionally accepts \code{"correlation"} (Pearson), \code{"spearman"}, and
\code{"kendall"} for \eqn{1 - |r|} correlation-based distances.
Defaults to \code{"euclidean"}.}

\item{hclust_method}{Agglomeration method passed to \code{\link[stats:hclust]{stats::hclust()}}.
Defaults to \code{"complete"}.}

\item{dist_fn}{Optional custom distance function. Should accept a matrix
(rows = observations) and return a \link[stats:dist]{stats::dist} object. When supplied
this overrides \code{dist_method}.}
}
\value{
A list with components \code{tree} (hclust object) and \code{order}
(character vector of names in dendrogram order).
}
\description{
Thin wrapper around \code{\link[stats:hclust]{stats::hclust()}} that returns both the tree and the
leaf order. Supports all \code{\link[stats:dist]{stats::dist()}} methods plus correlation-based
distances and fully custom distance functions.
}
\keyword{internal}
