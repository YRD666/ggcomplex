% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-main.R
\name{gg_matrix}
\alias{gg_matrix}
\title{Create a generic matrix plot with a custom geom}
\usage{
gg_matrix(
  mat,
  geom_fn = ggplot2::geom_tile,
  mapping = ggplot2::aes(fill = .data$value),
  fill_palette = c("#2166AC", "white", "#B2182B"),
  fill_label = "Value",
  fill_limits = NULL,
  fill_midpoint = 0,
  na_color = "grey50",
  scale_type = c("auto", "fill", "color", "none"),
  show_row_names = TRUE,
  show_col_names = TRUE,
  row_names_size = 8,
  col_names_size = 8,
  col_names_angle = 45,
  row_names_side = "left",
  col_names_side = "bottom",
  row_names_face = "plain",
  col_names_face = "plain",
  legend_position = "right",
  title = NULL,
  base_size = 11,
  panel_border_color = NA,
  panel_border_width = 0.5,
  panel_spacing = 2,
  extra_layers = list(),
  ...
)
}
\arguments{
\item{mat}{A numeric matrix. Row names = features, column names = samples.}

\item{geom_fn}{A ggplot2 geom function such as \code{ggplot2::geom_point}.}

\item{mapping}{An aesthetic mapping created by \code{\link[ggplot2:aes]{ggplot2::aes()}}. The
variables \code{col_id}, \code{row_id}, and \code{value} are available.}

\item{fill_palette}{Low/mid/high colours. Set to \code{NULL} to skip adding a
colour scale.}

\item{fill_label}{Legend title for the fill scale.}

\item{fill_limits}{Numeric vector of length 2 giving the value range to
display. Values outside this range are squished. \code{NULL} = auto.}

\item{fill_midpoint}{Numeric midpoint for diverging palettes. Defaults to 0.}

\item{na_color}{Colour used for \code{NA} cells. Defaults to \code{"grey50"}.}

\item{scale_type}{Which colour scale to add: \code{"fill"}, \code{"color"}, or
\code{"auto"} (inspect the mapping to decide). Set to \code{"none"} to skip.}

\item{show_row_names}{Logical; show row-axis text?}

\item{show_col_names}{Logical; show column-axis text?}

\item{row_names_size}{Font size for row names.}

\item{col_names_size}{Font size for column names.}

\item{col_names_angle}{Rotation angle for column names.}

\item{row_names_side}{\code{"left"} or \code{"right"}. Where to put row-axis labels.}

\item{col_names_side}{\code{"bottom"} or \code{"top"}. Where to put column-axis labels.}

\item{row_names_face}{Font face for row names (\code{"plain"}, \code{"bold"},
\code{"italic"}, \code{"bold.italic"}).}

\item{col_names_face}{Font face for column names.}

\item{legend_position}{Legend position: \code{"right"}, \code{"left"}, \code{"top"},
\code{"bottom"}, or \code{"none"}.}

\item{title}{Optional title string displayed above the main plot.}

\item{base_size}{Base font size for the theme.}

\item{extra_layers}{A list of additional ggplot layers (scales, themes).}

\item{...}{Additional arguments passed to \code{geom_fn()}.}
}
\value{
A \code{ggcomplex} S3 object.
}
\description{
The most flexible main-plot constructor. Supply a matrix and a geom
function, and the function builds a ggcomplex object with that layer.
}
