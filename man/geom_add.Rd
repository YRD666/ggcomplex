% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{geom_add}
\alias{geom_add}
\title{Add any subplot to the main plot (unified interface)}
\usage{
geom_add(
  obj,
  geom = c("bar", "tile", "dot", "boxplot", "violin", "line", "text", "histogram",
    "tree"),
  side = "right",
  data = NULL,
  mapping = NULL,
  size = NULL,
  palette = NULL,
  continuous_palette = NULL,
  fill = NULL,
  color = NULL,
  alpha = NULL,
  border_color = NA,
  border_width = 0.2,
  show_legend = TRUE,
  title = NULL,
  ylim = NULL,
  panel_border_color = NA,
  panel_border_width = 0.5,
  gap = NULL,
  ...
)
}
\arguments{
\item{obj}{A \code{ggcomplex} object.}

\item{geom}{Subplot type: \code{"bar"}, \code{"tile"}, \code{"dot"}, \code{"boxplot"},
\code{"violin"}, \code{"line"}, \code{"text"}, \code{"histogram"}, or \code{"tree"}.}

\item{side}{Position: \code{"right"} (default), \code{"left"}, \code{"top"}, or
\code{"bottom"}. For trees, \code{"left"} / \code{"right"} maps to \code{add_row_tree()},
\code{"top"} / \code{"bottom"} maps to \code{add_col_tree()}.}

\item{data}{A data frame for annotation types. Ignored for \code{geom = "tree"}.}

\item{mapping}{An aesthetic mapping created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.
Ignored for \code{geom = "tree"}.}

\item{size}{Relative size of the subplot panel. Maps to \code{width} for
left/right sides, \code{height} for top/bottom. \code{NULL} = sensible default.}

\item{palette}{Discrete colour vector.}

\item{continuous_palette}{2- or 3-colour gradient vector.}

\item{fill}{Fixed fill colour.}

\item{color}{Fixed colour.}

\item{alpha}{Transparency.}

\item{border_color}{Border colour for tile/bar geoms.}

\item{border_width}{Border line width.}

\item{show_legend}{Show this panel's legend?}

\item{title}{Small axis title for the annotation panel.}

\item{ylim}{Numeric length-2 vector limiting the value axis.}

\item{panel_border_color}{Panel border colour (\code{NA} = none).}

\item{panel_border_width}{Panel border line width.}

\item{gap}{Override spacing (pt) between this panel and the main plot.}

\item{...}{Further arguments forwarded to the underlying function
(e.g. \code{text_size}, \code{line_show_points}, \code{k}, \code{dist_method}, etc.).}
}
\value{
Updated \code{ggcomplex} object.
}
\description{
A single entry-point that wraps every annotation and dendrogram helper.
Call it repeatedly in a pipe to attach subplots of any type to any side.
}
\details{
The function determines row-side vs column-side from \code{side}:

\itemize{
\item \code{"left"} / \code{"right"} → row-side annotations / row tree
\item \code{"top"} / \code{"bottom"} → column-side annotations / column tree
}

For \code{geom = "tree"}, \code{data} and \code{mapping} are not required; all tree
parameters (\code{tree}, \code{order}, \code{dist_method}, \code{hclust_method}, \code{k}, etc.)
can be passed via \code{...}.
}
\examples{
\dontrun{
mat <- matrix(rnorm(100), 10, 10,
              dimnames = list(paste0("G", 1:10), paste0("S", 1:10)))
meta_r <- data.frame(gene = rownames(mat), score = runif(10))
meta_c <- data.frame(sample = colnames(mat), quality = runif(10, 70, 100))

gg_heatmap(mat) \%>\%
  geom_add("tree", side = "left", k = 3) \%>\%
  geom_add("tree", side = "top") \%>\%
  geom_add("bar", side = "right", data = meta_r,
           mapping = aes(x = gene, y = score), fill = "steelblue") \%>\%
  geom_add("dot", side = "top", data = meta_c,
           mapping = aes(x = sample, y = quality), color = "red")
}
}
