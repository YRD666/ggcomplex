% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-circular.R
\name{add_inner_links}
\alias{add_inner_links}
\title{Add inner chord links to a circular plot}
\usage{
add_inner_links(
  obj,
  links,
  from_var = "from",
  to_var = "to",
  color_var = NULL,
  color = "grey50",
  palette = NULL,
  continuous_palette = NULL,
  linewidth_var = NULL,
  linewidth = 0.5,
  linewidth_range = c(0.3, 2),
  alpha = 0.5,
  curvature = 0.7,
  n_points = 50,
  ...
)
}
\arguments{
\item{obj}{A \code{ggcomplex} object with \code{plot_type = "circular"}.}

\item{links}{Data frame with at least \emph{from} and \emph{to} columns whose
values match matrix column names.}

\item{from_var}{Column in \code{links} for the source position.}

\item{to_var}{Column in \code{links} for the target position.}

\item{color_var}{Optional column to map to link colour.}

\item{color}{Fixed colour when \code{color_var} is \code{NULL}.}

\item{palette}{Named colour vector for discrete \code{color_var}.}

\item{continuous_palette}{2- or 3-colour vector for continuous \code{color_var}.}

\item{linewidth_var}{Optional column to map to line width.}

\item{linewidth}{Fixed line width when \code{linewidth_var} is \code{NULL}.}

\item{linewidth_range}{Numeric range for mapped line widths.}

\item{alpha}{Transparency (0–1).}

\item{curvature}{How deeply curves dip toward the centre (0 = flat at
inner edge, 1 = passes through the very centre).}

\item{n_points}{Number of interpolation points per curve.}

\item{...}{Currently unused.}
}
\value{
Updated \code{ggcomplex} object.
}
\description{
Draws curved Bézier connections inside the inner circle, producing a
chord-diagram / network-graph effect. Each link connects two column
positions, curving through the centre of the circle.
}
