% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-tree.R
\name{add_row_tree}
\alias{add_row_tree}
\title{Add a row dendrogram to the composite plot}
\usage{
add_row_tree(
  obj,
  tree = NULL,
  order = NULL,
  dist_method = "euclidean",
  hclust_method = "complete",
  dist_fn = NULL,
  width = NULL,
  line_size = 0.5,
  color = "grey30",
  linetype = "solid",
  k = NULL,
  k_colors = NULL,
  cut_height = NULL,
  cut_linetype = "dashed",
  show_tip_labels = FALSE,
  tip_label_size = 2.5,
  gap = NULL
)
}
\arguments{
\item{obj}{A \code{ggcomplex} object created by \code{\link[=gg_heatmap]{gg_heatmap()}}.}

\item{tree}{Optional pre-computed tree object (\code{hclust}, \code{dendrogram}, or
\code{phylo}). When supplied the internal clustering step is skipped.}

\item{order}{Optional character vector giving a custom row order. Useful
when the tree has already been computed externally. If \code{tree} is given
without \code{order}, the order is derived from \code{tree}.}

\item{dist_method}{Distance method for \code{\link[stats:dist]{stats::dist()}}. Also accepts
\code{"correlation"}, \code{"spearman"}, \code{"kendall"}.}

\item{hclust_method}{Agglomeration method for \code{\link[stats:hclust]{stats::hclust()}}.}

\item{dist_fn}{Optional custom distance function (overrides \code{dist_method}).}

\item{width}{Relative width of the tree panel.}

\item{line_size}{Line width of dendrogram branches.}

\item{color}{Branch colour. Can be a single colour string, or a named
vector to colour branches by sub-tree (requires \code{k} to be set).}

\item{linetype}{Line type (\code{"solid"}, \code{"dashed"}, \code{"dotted"}, etc.).}

\item{k}{Number of clusters for branch colouring.}

\item{k_colors}{Character vector of \code{k} colours.}

\item{cut_height}{Optional height at which to draw a horizontal cut line.}

\item{cut_linetype}{Linetype for the cut line.}

\item{show_tip_labels}{Logical; show leaf/tip labels on the dendrogram?}

\item{tip_label_size}{Font size for tip labels.}
}
\value{
Updated \code{ggcomplex} object.
}
\description{
Performs hierarchical clustering on the rows of the data matrix, renders a
dendrogram with \code{\link[ggtree:ggtree]{ggtree::ggtree()}}, and inserts it into the left panel.
\strong{The main heatmap is automatically re-ordered} to match the tree leaves.
}
