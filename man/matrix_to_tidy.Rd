% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-utils.R
\name{matrix_to_tidy}
\alias{matrix_to_tidy}
\title{Convert a matrix to tidy (long) format}
\usage{
matrix_to_tidy(mat, row_order = rownames(mat), col_order = colnames(mat))
}
\arguments{
\item{mat}{A numeric matrix with row names and column names.}

\item{row_order}{Character vector giving the desired row order.
Defaults to \code{rownames(mat)}.}

\item{col_order}{Character vector giving the desired column order.
Defaults to \code{colnames(mat)}.}
}
\value{
A tibble with columns \code{row_id}, \code{col_id}, and \code{value}.
}
\description{
Transforms a numeric matrix into a three-column tibble suitable for
ggplot2 mapping. Row and column names are preserved as ordered factors
whose levels follow the original matrix order (or a custom order if
clustering has been applied).
}
\keyword{internal}
